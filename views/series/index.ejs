<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <title>MagicM</title>
</head>
<body>
  <div class="container">
    <h1 class="mb-4">Series</h1>
    <a href="/series/new" class="btn btn-success">Add Series</a>

    <% series.forEach(series => { %>
      <div class="card mt-4">
        <div class="card-body">
          <h4 class="card-title"><%= series.title %></h4>
          <div class="card-subtitle text-muted mb-2">
            <%= series.createdAt.toLocaleDateString() %>
          </div>
          <div class="card-text mb-2"><%= series.description %></div>
          <a href="<%= series.googledrive %>" class="btn btn-primary">Google Drive</a>
          <a href="<%= series.mega %>" class="btn btn-danger">Mega</a>
          <a href="series/edit/<%= series.id %>" class="btn btn-info">Edit</a>
          <form action="/series/<%= series.id %>?_method=DELETE" method="POST" class="d-inline">
            <button type="submit" class="btn btn-warning">Delete</button>
          </form>
        </div>
      </div>
    <% }) %>
  </div>
</body>
</html>